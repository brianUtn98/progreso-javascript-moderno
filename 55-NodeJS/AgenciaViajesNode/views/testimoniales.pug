extends ./layout/index

block contenido
    main.container.mt-5 
        h1.text-center.mt-5 #{pagina}
        
        blockquote.text-center Lee sobre nuestros clientes y sus experiencias
        div.row.testimoniales
            each testimonial in testimoniales
                div.col-md-6.col-lg-4.mb-4
                    div.card
                        div.card-body
                            blockquote.blockquote
                            p.mb-0
                                | #{testimonial.mensaje}
                            footer.blockquote-footer
                                | #{testimonial.nombre}

        div.row
            div.col-md-12
                h2.d-block.w-100.text-center Agrega un testimonial

                if(errores)
                    each error in errores
                        div.alert.col.alert-danger.text-center #{error.mensaje}

            div.row.justify-content-center.col
                div.col-md-8
                    form(action="/testimoniales" method="POST")
                        .form-group
                            label(for="nombre") Nombre  
                            input#nombre(type="text",placeholder="Tu nombre", name="nombre",class="form-control",value=nombre)
                        .form-group
                            label(for="correo") Correo electrónico  
                            input#nombre(type="text",placeholder="ejemplo@dominio.com", name="correo",class="form-control",value=correo)    
                        .form-group
                            label(for="mensaje") Mensaje  
                            textarea#mensaje(name="mensaje", class="form-control",placeholder="Tu mensaje",rows=3)= mensaje 

                        input.btn.btn-success.btn-block(type="submit")