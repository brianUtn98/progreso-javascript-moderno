const numero1 = 20;
const numero2 = "20";

console.log( parseInt(numero2)) // esto es una funcion

console.log(numero1.toString()) // esto es un método

//El concepto método tiene que ver con la POO. Un método es propio de un objeto, es un mensaje que él entiende.
